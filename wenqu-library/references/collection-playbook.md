# 素材收集执行手册

四步流程（规划 → 搜索 → 下载 → 整理）每一步的具体执行方法。`SKILL.md` 是概览，本文件是操作细则。

## 第 1 步：规划

**输入**：题目方向、写作目标、目标读者、范围锁定、已明确的关键词与模块名（来自 wenqu-write Step 1/1.5，或用户直接给出）。

**产出：收集清单**（内容范围的唯一确认点），格式：

```markdown
## 收集清单

### 相似文章（写法参考，找 2~4 篇）
| 想找什么 | 搜索关键词 |
| --- | --- |
| 讲 XX 系统的技术长文，结构偏源码拆解 | "XX 源码解析"、"XX deep dive"、"XX 原理" |
| 面向中级工程师的 XX 入门到实践 | "XX 入门 实践"、"getting started XX production" |

### 相关素材（事实素材，按需）
| 想找什么 | 搜索关键词 | 预期来源 |
| --- | --- | --- |
| XX 的官方设计文档 | "XX design doc"、"XX architecture" | 官网与 GitHub |
| XX 的容量与性能数字 | "XX benchmark"、"XX 性能测试" | 官方博客与论文 |
| 公众号上的 XX 实践分享 | "XX 实践 公众号" | mp.weixin.qq.com |
```

执行要点：

1. **相似文章先于相关素材**——先看别人怎么切题，再决定还缺什么料。
2. 关键词中英文各准备 2~3 组，覆盖同义表达。
3. 清单给用户确认时说明预计抓取数量；范围锁定里明确排除的内容不安排搜索。
4. CLI 授权与升级独立于内容范围确认：首次使用前请求一次明确授权（安装、必要时下载浏览器），已授权并验证通过的版本在后续任务直接复用；不自动联网升级，升级由用户主动发起。同一任务内只请求一次授权。

## CLI 检查、授权与降级

每次收集流程开始时，agent 先运行 `wenqu doctor --json` 检查受管运行环境（健康检查不改变状态）；`wenqu-cli` 包含 Python 依赖，Chromium 是唯一需要显式下载的大型依赖。

- `wenqu doctor --json` 显示 ready：用 `wenqu library search`（搜索补充）与 `wenqu library fetch`（下载主路径）。两者在已授权前提下作为默认动作执行，不静默跳过。
- 浏览器缺失：agent 先运行 `wenqu setup library --dry-run --json` 展示将下载的内容；得到用户明确授权后执行 `wenqu setup library`，再运行 `wenqu doctor --json` 验证。
- `wenqu` 缺失、安装或浏览器设置失败：agent 不要求用户复制命令、配置 PATH 或排障；搜索继续用 agent 自带联网搜索工具，下载回退到 agent 自带抓取（**仅作回退，不是常规路径**），并把不可用原因写入本轮失败记录。不得把"未主动发起授权"当作"CLI 不可用"而默认跳过 CLI 抓取。

不要安装或启动第二套搜索/抓取 CLI、daemon、MCP 或单独的 Crawl4AI CLI；它们不再是 Wenqu 文库的运行时契约。

## 第 2 步：搜索与候选合并

1. **先跑 CLI 搜索**：CLI 已授权并 ready 时，对清单中每组关键词运行 `wenqu library search`（多引擎候选，默认搜索路径，不静默跳过）；解析 JSON envelope，将结果记录为 `wenqu-cli:{engine}`，`channel` 不是 `direct` 时追加为 `wenqu-cli:{engine}:{channel}`。每组取前 3~5 条，记录标题、URL、一句话相关性。单个搜索引擎失败时保留其他引擎结果和失败信息。
2. **CLI 不可用时再跑原生搜索补充**：CLI 未授权、未安装、整体失败或结果不足时，用 agent 自带的联网搜索工具按相同关键词补充候选，检索渠道记为 `agent-native`，每组取前 3~5 条；CLI 失败原因写入记录，但不阻塞收集。
3. **受管浏览器回退**：CLI 默认只会为百度、必应、Brave 与搜狗在直连失败或空结果时执行一次受限的 Crawl4AI 回退；结果 `channel` 为 `browser`。验证页、登录页、付费墙、空结果或无法还原原始 URL 时只记录失败，不反复重试，也不绕过访问控制。需要禁用回退时传 `--no-browser-fallback`。
4. **统一去重后再下载**：合并原生、直连补充和浏览器恢复候选，先按规范化 URL 去重，再按内容标题与正文摘要辅助识别同文转载。规范化时移除 fragment 与常见追踪参数；不得改写带签名的 URL 或微信公众号 `src=11` 入口。重复项保留一条候选，并合并全部检索渠道。
5. **统一分级**：官方文档与源码 > 官方博客 > 知名技术媒体 > 个人博客 > 内容农场；同内容转载优先保留原始出处。只在合并、去重和分级后安排下载，不再让用户逐条确认。
6. 用户直接提供 URL 时不做检索；该条标为 `用户提供`，仍与搜索候选一起去重和分级。
7. 搜索连续两轮找不到满意结果时，在清单上标注“未找到，待用户补充”，不硬凑低质量来源。
8. **论文与研究报告例外**：先由 agent 原生搜索按题名、作者、DOI、会议或期刊和年份定位论文 PDF 或官方落地页；Wenqu CLI 没有学术专用引擎，只能补充解读性文章与外围候选。具体引擎范围见 `wenqu-cli.md`。

## 第 3 步：下载

用 `wenqu library fetch` 作为**默认抓取路径**（Crawl4AI 封装其中，是本技能唯一的受管抓取链路），产物直接写入本篇素材目录对应分类下；完整命令、引擎范围、浏览器边界与失败处理见 `wenqu-cli.md`。

```bash
# 单页
wenqu library fetch <url> --out {materials}/articles/<域名>/<slug>.md --json

# 整站与文档站（产物是合并单文件，====== 分隔）
wenqu library fetch <url> --max-pages <N> --out {materials}/docs/<站点>.md --json
```

- CLI 已授权并 ready 时，下载默认走 `wenqu library fetch`，不静默跳过；CLI 不可用、健康检查失败或某页抓取失败时，agent 回退到原生下载（**仅作回退**），并在 index.md 登记回退原因与实际下载方式；不能因 CLI 失败放弃该候选，也不能把"未发起授权"当作默认跳过 CLI 的理由。
- 批量下载逐个串行执行，避免浏览器实例争抢资源或触发反爬。
- 边抓边登记：每成功一项即写 URL、检索渠道与文件路径；失败也登记 URL、实际下载方式和原因；回退到原生下载的同样登记回退原因。
- 文件命名：`articles/` 下按来源域名建二级目录，文件名用内容 slug，不用随机串。

## 第 4 步：整理

1. 通读下载产物写摘要，写清“讲了什么、对本篇有什么用”，不抄标题。
2. 登记 `index.md`：按 `SKILL.md` 填全来源类型、来源、**检索渠道**、文件路径、用途、标签和关联章节。
3. 与全局文库和本篇已下载内容去重；下载产物重复时只保留一份文件，但在素材条目的检索渠道列保留所有发现路径。
4. 可跨文章复用的条目提炼成 L 条目回收至全局文库；原始文件仍留在本篇素材目录。
5. 收尾向用户简要汇报：

   - 原生搜索与增强搜索各带来多少候选
   - 合并后保留多少素材
   - 下载成功与失败数量
   - 增强 CLI 是否启用
   - 失败清单
