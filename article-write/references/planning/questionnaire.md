# 项目问卷生成指南

**必须使用 `AskUserQuestion` 工具交互式提问，不得把所有问题以文本一次性列出。**

---

## 整体流程

```
检查全局画像
    ↓
  存在 → 展示给用户确认 → 有修改则更新 → 进入文章专属问卷
  不存在 → 问前说明 → 建立全局画像 → 进入文章专属问卷
    ↓
文章专属问卷（扫系统后生成，扫描证据当场记入 materials.md）
    ↓
写入 SKILL.md（索引+关键信息）+ references/materials.md（素材库）+ references/preferences.md（偏好与反馈库，随时追加）
```

---

## 全局画像

### 路径（优先级从高到低）

| 优先级 | 路径 | 说明 |
|--------|------|------|
| 1 | `.gogoingai/article-write/profile.md`（项目目录） | 项目级覆盖 |
| 2 | `$HOME/.gogoingai/article-write/profile.md` | 用户全局 |

通常写在用户全局路径，只有特殊项目才用项目级覆盖。

### 检查与加载

**有全局画像时**：把画像内容展示给用户，一句话说明它的作用，然后用 `AskUserQuestion` 确认：

> 「我找到了你之前保存的读者/作者画像，用它来规划这篇文章会更准确。请确认它是否还适用：」
>
> 展示画像内容（读者背景、作者背景、语言偏好）
>
> 然后问：「这个画像还准确吗？」
> - A) 完全准确，直接用
> - B) 大部分准确，需要小改
> - C) 需要重新建立

- **A** → 直接进入文章专属问卷
- **B** → 用 `AskUserQuestion` 只问需要修改的部分，更新画像文件
- **C** → 走完整建立流程

**没有全局画像时**：先说明再建立（见下方"问前说明"）。

### 格式

```markdown
# 读者/作者画像

## 典型读者背景
| 维度 | 程度 |
|------|------|
| 技术背景 | [无 / 初级工程师 / 中级工程师 / 高级工程师] |
| AI/LLM 熟悉度 | [不了解 / 知道概念 / 用过工具 / 深度使用或开发] |
| 分布式/系统设计 | [不了解 / 知道基础 / 有经验] |
| 阅读目的 | [了解系统 / 学习设计 / 技术选型 / 深度研究] |
| 读完后的期望结果 | [知道系统能做什么 / 能判断是否值得研究 / 能上手使用 / 能参与开发] |

## 作者技术背景
| 领域 | 熟悉程度 |
|------|---------|
| [领域1] | [了解概念 / 实际使用 / 深度开发] |
| [领域2] | ... |

## 写作动机
- 典型场景：[对外推广 / 内部沉淀 / 技术分享 / 个人总结]
- 发布渠道：[个人博客 / 公司博客 / 团队内部文档 / newsletter / 演讲稿]

## 默认偏好
- 语言：[中文 / 英文]
- 文章深度：[功能层 / 设计层 / 实现层]
- 文章模式：[A 快速入门 / B 架构深挖 / C 方案对比]
```

---

## 问前说明

**没有全局画像时，建立画像前必须先说明：**

> 「在开始规划这篇文章之前，我想先了解一下你的典型读者是谁、你自己的技术背景是怎样的。
>
> 这些信息会保存成一个画像文件，以后写其他文章时直接加载，不用重复回答。
>
> 我会分几轮问几个问题，每轮 2~3 题，回答完就开始规划。」

然后用 `AskUserQuestion` 开始提问。

---

## 建立全局画像（分 2 轮）

### 第 1 轮：典型读者背景 + 写作动机（4~5 题）

```
AskUserQuestion({
  questions: [
    {
      question: "你的文章通常面向什么技术背景的读者？",
      header: "读者技术背景",
      options: [
        { label: "非技术读者", description: "不写代码，只关心系统能做什么" },
        { label: "初中级工程师", description: "会写代码，不熟悉这类系统的设计" },
        { label: "高级工程师", description: "有系统设计经验，关注取舍和细节" }
      ]
    },
    {
      question: "读者对 AI/LLM 工具（如 Claude、GPT、Cursor）的熟悉程度？",
      header: "AI 熟悉度",
      options: [
        { label: "不了解", description: "没有用过" },
        { label: "知道概念", description: "知道 AI 是什么，没深入用" },
        { label: "日常使用", description: "每天用，熟悉基本行为" },
        { label: "深度开发", description: "基于 LLM 开发过应用或工具" }
      ]
    },
    {
      question: "读者读完这篇文章，你希望他们能做到什么？",
      header: "读完期望",
      options: [
        { label: "知道它能做什么", description: "了解系统存在和解决的问题，不需要深入" },
        { label: "能判断是否值得研究", description: "有足够信息决定是否继续深入" },
        { label: "能上手使用", description: "读完就能开始用，不用额外查文档" },
        { label: "能参与开发", description: "理解实现细节，能基于它二次开发" }
      ]
    },
    {
      question: "你写这类技术文章的典型动机是什么？",
      header: "写作动机",
      multiSelect: true,
      options: [
        { label: "对外推广", description: "让更多人了解这个项目或系统" },
        { label: "内部沉淀", description: "团队内部知识传递或新人文档" },
        { label: "技术分享", description: "博客、演讲、newsletter 等公开内容" },
        { label: "个人总结", description: "记录自己对系统的理解和研究" }
      ]
    }
  ]
})
```

### 第 2 轮：作者背景 + 默认偏好（3 题）

```
AskUserQuestion({
  questions: [
    {
      question: "你自己对 AI 系统底层（Agent、上下文管理、记忆等）的熟悉程度？",
      header: "作者 AI 深度",
      options: [
        { label: "了解概念", description: "知道是什么，没深入研究实现" },
        { label: "实际使用", description: "用过相关工具，知道行为" },
        { label: "读过源码", description: "研究过实现细节" },
        { label: "参与开发", description: "开发过类似系统" }
      ]
    },
    {
      question: "你写文章通常用什么语言？",
      header: "语言偏好",
      options: [
        { label: "中文", description: "" },
        { label: "英文", description: "" },
        { label: "两者都有", description: "视项目而定" }
      ]
    },
    {
      question: "你偏好的文章深度？",
      header: "默认深度",
      options: [
        { label: "功能层", description: "讲清楚能做什么、怎么用，跳过实现" },
        { label: "设计层", description: "讲清楚为什么这样设计，含关键取舍" },
        { label: "实现层", description: "深入参数、公式、源码" }
      ]
    }
  ]
})
```

回答完后，写入全局画像文件，告知用户保存路径。

---

## 文章专属问卷（扫系统后生成）

全局画像确认/建立完后，**先用 Grep/Read 扫该系统，找到 3~5 个核心机制各自的具体文件和行号**，再生成 **6~10 个系统专属问题**，分 2~3 轮用 `AskUserQuestion` 问完。

**扫描是硬性前提，不是可选步骤**：不能凭项目名或 README 一句话描述就出题。扫完确实找不到某个机制的实现，如实说明「扫了 [目录]，没找到 [机制] 的实现，当作待确认处理」，不要假装扫过。

**扫到的每条证据当场追加进 `references/materials.md` 的『源码证据』表**（格式见文末「写入 materials.md」一节），不要等问卷问完、骨架都要出了才回头凭记忆补——扫描和记录是同一个动作。

**问题维度：**

### A0. 背景深挖（用户主动提供背景信息时必做，通用判定能力，不是固定模板）

**这是一种判定能力，不是绑定某个场景的固定问题清单。** 不要因为素材是"改写已发布文章避免查重"就套用一组写死的选项——换成"内部资料要对外发布""老文章要翻新""竞品最近发了类似内容所以要调整角度"，问题的具体内容都会不一样。判定标准只有一条：

> **用户主动说出的每一句背景信息，背后通常还有没说出口的"为什么"和"卡在哪"。听到背景信息只是记下来，不等于理解了背景。**

**识别信号**（出现任意一种，就要深挖，不能只当参考资料收下）：
- 用户提到"这个之前发过/写过/做过"——判断标准是"发过"这件事本身会不会限制这次的写法（读者会不会有印象、渠道是否相同、时间隔了多久）
- 用户提到明确的外部要求（"公司要求""领导让我""这是任务"）——判断标准是这个要求背后有没有硬约束（deadline、必须包含的内容、不能触碰的红线），用户往往只说了要求本身，没主动说约束
- 用户提到"避免 XX"（避免重复、避免太深、避免太长）——判断标准是"避免"针对的是谁的感受（读者会不会看腻、审核会不会打回、平台会不会判重），不同对象决定不同应对策略
- 用户提供的素材本身不完整（"梦境部分我后面发你""这块先空着"）——判断标准是缺失的这部分是可选内容还是这篇文章成立的必要条件；如果是必要条件，deadline 逼近但素材未到位时该怎么办，必须现在就问清楚，不能拖到真遇到才慌

**深挖的做法**：识别到上述信号后，**现场根据这句背景信息本身生成 1~3 个针对性问题**（用 `AskUserQuestion`），问题和选项都要贴着这句话的具体内容写，不能套用别的场景的选项。以下三个方向可以作为组织问题时的检查清单（不是必须逐条问，而是判断"这句背景信息里，这三个维度分别答清楚了没有"）：

1. **动机/为什么**：这件背景事实存在的原因是什么？会不会影响文章该写成什么样？
2. **硬约束**：有没有回避不了的边界条件（时间、渠道、必须/不能包含的内容）？
3. **未决风险**：如果某个前提没兑现（素材没到、时间不够、约束有冲突），谁来做决定——是现在就问清楚"到时候怎么办"，还是可以先搁置到真正发生时再问？**能现在问清楚的，不要拖到执行阶段才发现要靠自己拍板。**

回答写入 `SKILL.md` 最前面新增的 `## 背景` 字段（见下方模板），字段内容根据这次深挖出来的具体维度组织，不套固定表格列。深挖过程中用户主动甩出的具体素材（截图要点、原文片段等）额外按条目追加进 `references/materials.md` 的『用户提供的素材』表。

### A. 读者对该系统核心概念的熟悉程度（3~5 题）

针对这个系统最重要的 3~5 个概念各出一题，**问之前先引用扫描找到的 `path:line`**，选项统一为：
- 没听说过 / 知道大概 / 用过接触过 / 深入了解

例：不问"你对缓存机制熟悉吗"，改问"`cache.py:42` 用的是 LRU 淘汰策略，你对这个策略熟悉到什么程度？"——引用具体证据比泛泛提概念名更容易让用户校准回答。

### B. 本篇写作目标（3~4 题）

- **写作动机**：这篇文章为什么要写？和你的全局典型场景有什么不同？
- **读者起点**：读者读这篇文章之前，对这个系统了解多少？（完全不知道 / 听说过名字 / 知道能做什么 / 用过但不了解原理）
- **读完期望**：读者读完这篇文章，你希望他们能做到什么？（可能和全局偏好不同）
- **文章模式**：侧重哪个方向？（A 快速入门 / B 架构深挖 / C 方案对比）
- **发布渠道**（可选）：会发布在哪里？（影响术语解释密度和语气）

### C. 作者对该系统各模块的了解（3~4 题）

针对该系统主要模块各出一题，选项统一为：
- 需要你来研究 / 大致了解 / 比较清楚 / 读过源码

### D0. 范围锁定（必问，骨架前对齐，不可省略）

这一节的目标：在提骨架之前锁定"这次不写什么"，防止骨架阶段贪多、写到一半才发现摊子太大要返工。

```
AskUserQuestion({
  questions: [
    {
      question: "这篇文章明确不覆盖哪些模块/细节？（基于扫描到的模块列表）",
      header: "范围排除",
      multiSelect: true,
      options: [
        // 逐个列出扫描到但明显和本篇写作目标无关的模块/细节
        { label: "[模块/细节 A]", description: "[为什么可以排除]" },
        { label: "[模块/细节 B]", description: "[为什么可以排除]" },
        { label: "全部都要覆盖，不排除", description: "" }
      ]
    },
    {
      question: "如果篇幅要砍半，拿掉哪些内容这篇文章仍然成立？",
      header: "最小可行版本",
      options: [
        // 基于系统核心机制列选项，标出"拿掉后文章还立得住"的那部分
        { label: "只保留 [核心机制 1/2]", description: "其余标记为可选，视篇幅决定" },
        { label: "都是核心，砍不动", description: "" }
      ]
    }
  ]
})
```

回答写入 `SKILL.md` 的 `## 范围锁定` 字段。骨架阶段如果想加回被排除的模块，先回来跟用户确认，不要默默加回去。

### D. 内容策略（必问，骨架前对齐，避免反复改稿）

这一节的目标：在提骨架之前把"怎么写"对清楚，防止骨架提完再一轮一轮改深度、改例子、改结构。

**D1. 各主要章节的期望深度**

针对该系统的主要章节（扫系统后确定），逐章问期望深度：

```
AskUserQuestion({
  questions: [
    {
      question: "【章节名】这一章你希望写到哪个层次？",
      header: "章节深度",
      options: [
        { label: "简写带过", description: "说清楚是什么、做什么，不展开细节" },
        { label: "设计层", description: "重点说为什么这样设计，含主要取舍，跳过公式" },
        { label: "实现层", description: "展开参数、公式、源码逻辑" },
        { label: "视内容而定", description: "整体设计层，但某些核心机制展开到实现层" }
      ]
    }
    // 每个主要章节一题，合并在一次 AskUserQuestion 调用里（最多 4 题一组）
  ]
})
```

**D2. 例子策略**

```
AskUserQuestion({
  questions: [
    {
      question: "全文用一个贯穿始终的主线例子，还是每章用最合适的独立例子？",
      header: "例子策略",
      options: [
        { label: "单一主线例子", description: "同一个场景从头跟到尾，读者不用切换上下文" },
        { label: "章节独立例子", description: "每章选最能说明该机制的例子，互不关联" },
        { label: "主线 + 局部补充", description: "大框架用主线，某些机制用更合适的局部例子" }
      ]
    },
    {
      question: "你有偏好的例子场景吗？（比如：代码重构、测试规范、部署流程、团队协作规则……）",
      header: "例子偏好",
      options: [
        { label: "你来建议", description: "根据系统特性选最合适的" },
        { label: "有偏好，见文字输入", description: "" }
      ]
    }
  ]
})
```

**D3. 复杂内容的处理偏好**

```
AskUserQuestion({
  questions: [
    {
      question: "遇到复杂内容（公式、多步流程、抽象机制），优先用哪种方式帮读者理解？",
      header: "复杂内容处理",
      multiSelect: true,
      options: [
        { label: "多画图", description: "流程图、漏斗图、时序图辅助" },
        { label: "多举例", description: "每个抽象机制配一个具体数字例子" },
        { label: "直觉解释优先", description: "先说白话，再给公式" },
        { label: "完整推导", description: "公式来源和计算过程全部展开" }
      ]
    },
    {
      question: "多个章节涉及相似机制时（比如轻度和 REM 都有梦境日记），怎么处理重复内容？",
      header: "重复内容处理",
      options: [
        { label: "第一次详写，后面一句话带过", description: "「同上，不重复」" },
        { label: "每次都写，但侧重差异", description: "强调和前面有什么不同" },
        { label: "合并成一节统一讲", description: "两个地方共用一节" }
      ]
    }
  ]
})
```

以上问题回答完后，将结果写入 `SKILL.md` 的 `## 内容策略` 字段，再开始出骨架。

### D4. 写作画像（必问，骨架前对齐）

→ 查 `article-profile.md`。

这一步不重复询问已经确认的读者、写作动机和发布渠道，只补齐会改变写法的四件事：

- **题材：** 自研技术方案介绍、源码解析/外部项目介绍、论文/研究报告、产品说明/使用指南，或明确主次的混合题材；
- **核心目标：** 希望读者最终理解、判断或完成什么；
- **叙事主体：** 关键选择由谁作出，机制描述应由什么具体对象承担；
- **表达尺度：** 哪些地方应直白解释，哪些术语、公式或论证可保留。

用户已经给出明确信息时，整理成一句写作画像请用户确认即可；信息不足时，只补问 1～2 个决定性问题。答案写入 `SKILL.md` 的 `## 写作画像`，并作为 R6 的审查基准。

### E. 补充（必问，最后一轮单独问）

```
AskUserQuestion({
  questions: [
    {
      question: "还有什么要补充的吗？比如：某个模块特别想深入、想避开的内容、之前写过类似文章的经验、对这篇文章有什么特别的要求。",
      header: "补充说明",
      options: [
        { label: "没有，按上面的答案写就好", description: "" },
        { label: "有，见下方文字输入", description: "选这个后在文字框里补充" }
      ]
    }
  ]
})
```

用户选"没有"则跳过；选"有"或直接输入文字，则将内容写入 `SKILL.md` 的 `## 特别说明` 字段。

---

## 写入 SKILL.md

系统专属问卷回答完后，把结果写入 `{项目根目录}/.article-skills/{文件名}/SKILL.md`（项目根目录 = `git rev-parse --show-toplevel`，不是 git 仓库则用当前工作目录；文件名 = 本篇文章的文件名，不含扩展名）。

**SKILL.md 必须自包含**：全局画像当时确认好的内容要**整段拷贝**进下面模板的「读者画像」一节，不是写一句"参见全局画像"就跳过——本篇的写作依据不应该依赖全局画像文件之后还存在、还没被改动。

**新建时同步生成文章标识**：格式 `aid-` + 8 位十六进制随机数（`openssl rand -hex 4`），写进下面模板顶部的标识行，同时在文章正文 H1 标题下方插入引用块 `> 🔖 本文由 [wenqu-skills](https://github.com/gogoingai/wenqu-skills) 项目生成草稿 · 标识 \`aid-xxxxxxxx\`。发布前会自动移除本段。`——两处同时写，缺一不可，详见 `SKILL.md`「文章标识与重命名」一节。

```markdown
# 写作上下文

> 标识：`aid-xxxxxxxx` · 对应文章文件：`{当前文件名}.md`

## 索引
- [素材库](references/materials.md)
- [骨架](references/skeleton.md)
- [版本记录](references/changelog.md)
- [变更影响记录](references/change-impact.md)
- [术语表](references/terms.md)


## 背景
[仅当 A0 深挖出实质内容时才写这一节，没有背景信息可挖就省略。内容不套固定表格列，按 A0 挖出的具体维度组织，通常包括：
- 背景事实本身（用户说了什么）
- 动机/为什么（这个事实为什么存在，会怎么影响写法）
- 硬约束（时间/渠道/必须或不能包含的内容）
- 未决风险与处理方式（哪些前提没兑现时要停下来问用户，不能自己拍板）]

## 系统
[系统名称和一句话定位]

## 文章语言
[中文 / 英文]

## 文章模式
[A / B / C] — [一句话说明]

## 写作画像
- 题材：[自研技术方案介绍 / 源码解析 / 论文或研究报告 / 产品说明或使用指南 / 混合，注明主次]
- 核心目标：[读者最终应理解、判断或完成什么]
- 叙事主体：[关键选择如何表达；机制描述由哪些具体对象承担]
- 表达尺度：[术语、公式、案例与直白解释的取舍]

## 写作目标
[根据本篇回答整理成一句话：动机 + 读完期望]

## 发布渠道
[个人博客 / 公司博客 / 团队内部 / newsletter / 演讲稿 / 未定]

## 读者画像（全局画像快照，创建时整段拷贝 + 本篇补充）
| 维度 | 程度 |
|------|------|
| 技术背景 | ... |
| AI 熟悉度 | ... |
| 读这篇文章前对该系统的了解 | [完全不知道 / 听说过名字 / 知道能做什么 / 用过但不了解原理] |
| 读完后的期望结果 | ... |
| [系统专属概念1] | ... |
| [系统专属概念2] | ... |

## 作者知识储备
| 模块 | 了解程度 |
|------|---------|
| [模块1] | ... |
| [模块2] | ... |

## 范围锁定
- 明确不覆盖：[模块/细节列表]
- 最小可行版本（篇幅砍半时保留）：[核心机制列表]

## 内容策略
- 各章节深度：[章节名 → 简写/设计层/实现层/视内容而定，逐章列出]
- 例子策略：[单一主线 / 章节独立 / 主线+局部补充]
- 例子偏好：[场景描述]
- 复杂内容处理：[多画图 / 多举例 / 直觉优先 / 完整推导，可多选]
- 重复内容处理：[第一次详写后带过 / 侧重差异 / 合并成节]

## 特别说明
[用户补充的任何其他要求]
```

---

## 写入 materials.md

素材库落在 `{项目根目录}/.article-skills/{文件名}/references/materials.md`，是 Step 1 扫描、Step 2 深入阅读、用户直接提供的背景素材、article-translate 产出的所有素材的**唯一持久化落点**——不是写完骨架就作废的中间产物，后续审查、用户追问细节都直接查这份文件。

**边收集边写，不要攒到最后一次性补**：Step 1 扫到一条证据就追加一行；Step 2 读到一个数字、一个易误解点，当场补进对应表；用户甩来一段背景素材，当场记一行。每条素材一个唯一 ID（`M1`、`M2`……递增，不复用），骨架引用素材时标注这个 ID。

```markdown
# 素材库

## 源码证据
| ID | 机制/概念 | 出处(path:line) | 摘要 | 关联章节 |
|----|----------|-----------------|------|---------|
| M1 | LRU 淘汰策略 | cache.py:42 | ... | 待定 |

## 关键数字
| ID | 数字 | 出处(path:line) | 说明 | 关联章节 |
|----|------|-----------------|------|---------|
| M5 | 容量 128MB | config.py:10 | 默认缓存上限 | 待定 |

## 易误解点
| ID | 描述 | 出处(path:line) | 关联章节 |
|----|------|-----------------|---------|
| M9 | ... | ... | 待定 |

## 冲突裁决
| Claim / 概念 | 冲突来源 | 采用依据 | 裁决结果 | 受影响章节 |
|--------------|----------|----------|----------|------------|
| ... | 源码 M1 与设计稿 M12 描述不同 | [用户确认 / 当前代码 / 明确版本说明] | 正文采用……，另一版本说明为…… | 2.1, 4.3 |

## 用户提供的素材
| ID | 摘要 | 来源 | 关联章节 |
|----|------|------|---------|
| M8 | ... | 用户消息 [日期] | 待定 |

## 外部素材（翻译/引用）
| ID | 来源 | 摘要 | 关联章节 |
|----|------|------|---------|
| M10 | README 英文原文段落 | ... | 待定 |
```

**「关联章节」列**：Step 1/2 阶段先填"待定"，骨架定稿后回填对应章节号（骨架每节标注引用的素材 ID，是同一份对应关系的另一个方向）。

---

## 写入 preferences.md

偏好与反馈库落在 `{项目根目录}/.article-skills/{文件名}/references/preferences.md`，存放"这次说的话，下次同类操作还用得上"的持久化反馈——写法习惯、画图风格、术语取舍、审查侧重、翻译腔判定尺度等。**不再向用户提议修改技能仓库文档**，可复用的反馈一律记进这里，跟着这篇文章走。

判定标准、记录时机、加载方式见 `SKILL.md`「用户反馈与偏好持久化」一节。文件按来源分表：

```markdown
# 偏好与反馈库

## 写作偏好
| ID | 内容 | 来源 | 状态 |
|----|------|------|------|
| P1 | 代码示例统一用 TypeScript，不用伪代码 | 用户消息 [日期] | 生效 |

## 审查偏好
| ID | 内容 | 来源 | 状态 |
|----|------|------|------|
| P4 | "利用"一律改成"用"，不当翻译腔误判 | 用户消息 [日期] | 生效 |

## 翻译偏好
| ID | 内容 | 来源 | 状态 |
|----|------|------|------|
| P6 | 专有名词 "agent" 保留英文不译 | 用户消息 [日期] | 生效 |

## 配图偏好
| ID | 内容 | 来源 | 状态 |
|----|------|------|------|
| P8 | 默认用单色马克笔风格，箭头一律深灰色 | 用户消息 [日期] | 生效 |
```

每条一个唯一 ID（`P1`、`P2`……递增，不复用）。「状态」只有 `生效` / `已废弃` 两种——偏好被新指令覆盖或冲突时，把旧行状态改成`已废弃`（不删除，保留可追溯），新条目单独追加一行。「配图偏好」表由 article-image 技能读写，article-write 只在需要时查阅，不重复维护。

---

## 后续会话加载与补充提问

再次打开同一篇文章时：
1. 检查 `{项目根目录}/.article-skills/{文件名}/SKILL.md` → **有**则直接加载它（里面已含创建时拷贝的画像快照，不必再去读全局画像文件），告知用户「已加载上次对本篇的补充说明」；**没有但发现旧格式**（`.article-write/{文件名}.context.md` 或目录化之前的 `.article-skills/{文件名}.context.md`）→ 按 `SKILL.md`「上下文存储位置」一节的兼容规则迁移
2. 检查 `references/skeleton.md`、`references/materials.md`、`references/terms.md`、`references/change-impact.md`、`references/preferences.md` → 有则静默加载；有未闭环变更时在本轮收尾运行 R7，按已记录偏好执行，不重复问
3. `SKILL.md` **不存在且无旧格式文件**（本篇是第一次建立）→ 按前面的流程，先加载/建立全局画像，确认后拷贝一份快照写进新建的 SKILL.md

**补充提问**：规划中发现某维度信息不足，只用 `AskUserQuestion` 补问 1~2 题：
- 属于通用背景（读者习惯、语言偏好）→ 更新全局画像
- 属于本篇特有（这个系统的模块了解程度）→ 写入 `{项目根目录}/.article-skills/{文件名}/SKILL.md`
- 属于具体证据/数字/例子 → 追加进 `{项目根目录}/.article-skills/{文件名}/references/materials.md`
- 属于可复用的写法/审查/翻译/配图偏好 → 追加进 `{项目根目录}/.article-skills/{文件名}/references/preferences.md`
